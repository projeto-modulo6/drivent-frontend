import styled from 'styled-components';

export default function EventLocationContainer({ location }) {
  return <LocationContainer>Teste</LocationContainer>;
}

const LocationContainer = styled.div`
  display: flex;
  flex-direction: column;
  
`;
