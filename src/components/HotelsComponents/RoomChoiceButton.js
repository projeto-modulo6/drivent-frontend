import styled from 'styled-components';

export default function RoomChoiceButton({ room, name, capacity }) {
  return (
    <StyledEntry>
      <StyledButton></StyledButton>
    </StyledEntry>
  );
}

const StyledEntry = styled.li`
  display: flex;
  
`;

const StyledButton = styled.button``;
